export const Config = {
  DEMO_UI_MODE: false,
  // TEMPORARY: enables QA character/stage preview panel inside ProfileModal for TestFlight review.
  // Set back to false before submitting to the App Store public release.
  PREVIEW_QA_MODE: true,
} as const;
