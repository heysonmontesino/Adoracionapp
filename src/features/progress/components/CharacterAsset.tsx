/**
 * CharacterAsset
 *
 * Punto de entrada del personaje para el módulo de Progreso.
 * La lógica de render (3D / fallback) vive en ProgressCharacter3D.
 *
 * Este archivo existe como alias estable: si algún día el componente
 * necesita renombrarse o moverse, solo se cambia aquí.
 */
export { ProgressCharacter3D as CharacterAsset } from './ProgressCharacter3D'
